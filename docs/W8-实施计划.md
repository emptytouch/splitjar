# W8 实施计划 —— Agent 演示客户端 + 看板标注

> 上游 = [`开发计划.md`](开发计划.md) §W8(第 394–403 行)与 [`splitjar-product-spec.md`](splitjar-product-spec.md) §9.4 / §10 / §16.1。
> 本包**依赖 W7 已交付的服务端**(`/api/catalog` + `/api/content/:id` 的 402/交付两条路),
> W7 的计划与实测记录在 [`W7-实施计划.md`](W7-实施计划.md)。
>
> ⚠️ **本文件是方案,不是代码。** 与 W7 同一条纪律:先定方案,定了才开工。

---

## 〇、先结掉那个必须做的决策点：买家类型**怎么判**

§16.1 第 7 条要求「Agent 这笔购买在 `/dashboard` 中正确显示买家类型为"Agent"」。
但链上**没有**"买家类型"这个字段 —— 两条路走的是**同一个 `PaymentSplit` 事件**
（`shared/abi/creatorSplitter.ts:288`）。所以这一条全部难点就是:**凭什么说这笔是 Agent 买的?**

规格 §9.4 第 706 行给了两条路：

> ① Agent 付款方地址来自已知的演示密钥 / 客户端指纹,维护一个小白名单;
> ② 看板只区分"有钱包签名交互"与"无交互"两类。**MVP 用 ①,简单直接**。

### ⚠️ ② 站不住，而且是**方向性**的错

本项目这个 Agent 要**自己发两笔交易**（`approve` + `pay`）—— 它**有**钱包签名交互。
拿 ② 去判,它会被判成"人类"。**② 这条例子里没有可用的东西**,不要再讨论它。

### ③ 一条规格没写、但确实存在的信号（记录在案）

KV 里的 `payment:${txHash}` **只有 agent 那条路会写** ——
`api/content/[id].ts` 成功时调 `commitPayment`,而 `/api/unlock`（人类门禁）
**从不碰 `payment:*`**（已 grep 核实）。所以"这个 txHash 有没有落定记录"
**答的是**:"这笔内容是不是从 HTTP 402 那条路交付的"。

它有两个优点：**不可伪造**（要写它必须先在链上真付款）、**已经是现成的**
（`server/kv.ts:242` 的 `isPaymentConsumed` 就摆在那儿）。

**但它答的不是我们要问的那个问题**："从 402 交付"≠"agent 买的" —— 人当然也可以 `curl`。
而且它要**新基建**：看板现在是**纯链上**的（`useMyContents` 直接 `getLogs`，
不依赖任何服务端），接它就得新开一个端点、每次渲染多一轮 KV 读。

### ✅ 本包定：用 ① 白名单，并且**把白名单显示在界面上**

三条理由：

1. **与规格一致,且零额外基建。** 看板不用碰服务端 —— `useMyContents.ts:47` 的
   `Sale.payer` 已经有了,`DashboardPage.tsx:205` 已经在渲染 `来自 {shortAddress(s.payer)}`。
   **只差一个徽章**,不是一次架构改动。
2. **演示里 agent 用专属地址**（§一 决策 1）,所以标注**100% 准**,不会误标。
3. **白名单是"身份标注",不是"行为检测"—— 那就把它摊开给人看。**
   界面上直接列出名单内容。一个可审计的名单,比一个来路不明的"自动识别"更可信。

### ⚠️ 必须如实写进 UI 的代价

**一个不在白名单里的 agent 会被标成"人类"。** 这不是 bug,是这条路线的固有边界 ——
所以看板上那句话不能写成"自动识别买家类型",只能写**"按地址白名单判定"**。
这与 §5.2 那条纪律同源:**别把弱检查包装成强保护**。

### 备选（只在时间富余时做）

白名单 **+** KV 那个信号,两个都命中才标 "Agent",否则标"人类"。
成本是多一个端点 + 一次 KV 往返。**它换来的是"不可伪造"这一层**,
对演示没有增量,**明确排在 W11 之后**。

### ✅ 2026-09-24 拍板：走 **G0 白名单**，G2 记档备选

讨论中把这条岔口重新拉成了一条三档的梯子,拍板是按这条梯子拍的 —— 记下来:

| 档 | 做法 | 能证明什么 |
|---|---|---|
| **G0** | 地址白名单 | 「这个**付款人**是我们知道的 agent」 |
| G1 | 请求自报头(如 `User-Agent`),服务端记下 | 「这个**客户端自称**是 agent」—— 谁都能自封 |
| G2 | 用**付款那把密钥**签一句声明 | 「**付款人本人**声称这笔是自动发起的」,不可抵赖 |

**拍板结果**:本包用 **G0**,零额外基建,看板保持纯链上;
界面上**把名单摊开显示**,并写明"按地址白名单判定"。

⚠️ **为什么"自动识别"这件事到此为止** —— 链上**没有** agent 的痕迹:
两条路走同一个 `pay(bytes32)`、同一个 `PaymentSplit` 事件,而合约里
`msg.sender` **就是买家本人**（这正是我们不实现 x402 代付的理由,见
`shared/agentPay.ts` 文件头）,连"有人代发"这个区别都不存在。剩下的链上特征
（nonce 节奏 / gas 价格 / 有没有 approve）全是**启发式**:可伪造,还会把
"用脚本的老手"误判成 agent。**「是不是 agent」不是系统的属性,是客户端的属性** ——
一个脚本后面可能坐着人,一个人也可能 `curl`。**任何关于它的说法都只能是自报。**

**G2 记档**(将来若要讲"agent 身份可验证",这是现成的起点),三个前置缺一不可:

1. 一条 EIP-712 类型 + 服务端 `recoverTypedDataAddress`
2. ⚠️ **恢复出来的地址必须等于链上那个 `payer`** —— 没有这一步它退化成 G1
3. 一个**新端点** —— 看板是纯链上的(`useMyContents` 直接 `getLogs`),
   不开口子就永远读不到那条记录

### ⚠️ 一并否掉一个"看起来省钱、其实不省"的中间做法

曾经考虑过:本包**顺手让 agent 多带一个自报头、服务端记进 KV**,
"将来做 G2 时数据已经在攒了、不用回填"。

**这个做法站不住,已否掉。** 因为**G1 的头不是 G2 要的数据**:
G2 要的是一条**签名**,而签名必须**当场用付款人的密钥签**。
攒一堆无签名的自报头,将来一条也用不上 —— 那 20 行是白花的;
而真要攒签名,就等于把 G2 做到 90%,只差一个端点,那还不如直接做 G2。

**要么现在做 G2,要么不做,没有中间态。**

---

## 一、已定的决策（上游定的，本包照办）

### 决策 1：Agent 用自己的**专属测试地址**

上游依据：开发计划 §十 ——「**另备 2–3 个测试地址轮换**（Agent 脚本、人类买家、创作者各一个更清晰）」。
演示账户见 `contracts/README.md`：创作者 `0xA0b760…3D63`、人类买家 `0x9750Af…B988`。

**为什么必须分开**：Circle faucet 限 **1 USDC / 2 小时 / 地址**，
而现在在架那件演示内容 `0xe2f8dba7…` 的价格是 **0.1 USDC**（链上实测,`price=100000`）。
一次 drip 够 **10 次**付款 —— 看着宽裕,但 agent 脚本**演示现场要反复跑**,
和人类买家挤同一个地址就会互相耗额度,而且是**演示中途**才发现。

⚠️ **记一处上游文档与现状不一致**：开发计划 §R9 写的是「价格锁 **0.2 USDC**」,
而实际建出来的演示内容是 **0.1**。按实际值算额度,别按文档算。

### 决策 2：私钥只走**当前 shell 的环境变量**

依据是 `.env.example` 的原话与方案 §6.2：

> ⚠️ **绝不写进 `.env`** —— 这是部署私钥。只在当前 shell export,用完就关窗口。

三条硬约束,本包一条都不放宽：

- 不进 `.env` / `.env.local`（`vercel dev` 也不读后者,但那是另一回事）
- 不进仓库（`.gitignore` 之外还要靠人不手滑 —— 见 §七 风险）
- **不进前端产物**：`VITE_` 前缀会被**内联进 bundle**,所以 agent 的私钥
  **绝不能**有 `VITE_` 形式的名字
- `.env.example` 只登记**变量名与用法**,值永不出现（与现有那条红线一致）

### 决策 3：脚本落在 `scripts/`，且是 `.mjs`

依据 §W1 的定案：`scripts/` 必须是**第三处** ——
放 `src/` 会打进产物泄漏私钥,放 `api/` 会变成公开接口。

⚠️ **用 `.mjs` 不是随手选的**:`tsconfig.json` 的 `include` 是 `["src", "shared", "vite.config.ts"]`,
`tsconfig.api.json` 是 `["api", "server", "shared"]` —— **`scripts/` 两边都不进**。
`.mjs` 因此**不参与任何一次类型检查**,也就不会某天因为一个 tsconfig 放宽而把
Node 专用的东西（`node:crypto` / `process.env`）混进前端的编译单元。
（W7 已落地的 `scripts/verify-x402.mjs` 是同一条路子。）

### 决策 4：**报价必须从 402 响应里读出来**

§W8 的注意原文：

> 脚本里的"自主决策"要真实 —— **报价是从 402 响应里读出来的,不是硬编码**。
> 硬编码的脚本一看就假,而这是全场最难被复制的一屏,**被怀疑造假就全废**。

这一条是本包的**验收红线**,不是风格偏好。落地到三处（详见 §五）：
金额来自 `accepts[0].maxAmountRequired`、小数位来自 `/api/catalog` 的 `decimals`、
**要买哪一件**由脚本从 catalog 里挑。

### 决策 5：白名单**只有一个来源**，前后端读同一个文件

```
shared/agentAddresses.json     ← 唯一来源（纯数据）
shared/agentAddresses.ts       ← 前端用的带类型再导出（~5 行）
scripts/agent-buy.mjs          ← 直接读那个 .json
```

**为什么费这个劲**:如果脚本里写一份地址、前端里再写一份,
"脚本付的地址"和"看板标的地址"就会**各自漂移** —— 而漂移的症状是
**看板把 agent 标成人类**,恰好是 §16.1 第 7 条要验的那一屏。
两处各写一遍,等于把一条验收项交给"记得同步"。

`resolveJsonModule` 在 `tsconfig.json` 里已经开着（`true`）,前端 import 它不需要改配置;
Node 22+ 用 `import … with { type: 'json' }` 读同一个文件（`engines` 要求 `>=22.12.0`,满足）。

---

## 二、交付范围（逐条核过现状）

### 新增

| 文件 | 内容 |
|---|---|
| `scripts/agent-buy.mjs` | **演示主角**。一条命令跑完「发现 → 付款 → 取内容」,每步都打印 |
| `shared/agentAddresses.json` | Agent 地址白名单（唯一来源） |
| `shared/agentAddresses.ts` | 带 `Address[]` 类型的再导出,给前端用 |

### 修改

| 文件 | 改动 |
|---|---|
| `src/hooks/useMyContents.ts` | `Sale` 加一个 `isAgent: boolean`（由 `payer` 与白名单比对得出） |
| `src/pages/DashboardPage.tsx` | 付款人那行加徽章;**换掉第 222 行那句"是 W8"** |
| `src/pages/ConsolePage.tsx` | 第三个入口「看 Agent 自主购买」（见下） |
| `.env.example` | 登记 `AGENT_ADDRESS` 与私钥**变量名**（值永不出现） |
| `README.md` | 补 W8 进度 + **手动走一遍 agent 路径**的命令 |

### ⚠️ `ConsolePage.tsx` 里挂着一条 W7 没兑现的 TODO

`src/pages/ConsolePage.tsx:125` 现在写着：

> 第三个入口(看 Agent 自主购买)对应的是 W7 的 `/api/catalog`,现在做就等于挂一个假按钮。**W7 时改**。

W7 交了 API 但**没动这个入口**,所以它今天仍然是一个 `Placeholder`。
W8 把它填掉是**顺理成章**的:脚本一落地,"看 Agent 自主购买"就**有东西可看**了
（只读渲染 `/api/catalog` + 把运行命令原样贴出来）。

⚠️ **别做过头**:这个入口**不做成"在网页上点一下就跑 agent"**。
浏览器里没有 agent 的私钥,也没有 `spawn` —— 真要那样做就得把私钥搬到服务端,
直接违反决策 2。**它只负责展示与给命令。**

---

## 三、脚本怎么跑（一条命令，每一步都要看得见）

`开发计划.md:400` 明说：「脚本要**打印每一步**,因为它是演示主角,过程要能被看见。」
所以下面的每一步都有对应的输出。

```
node scripts/agent-buy.mjs                 # 完整跑
node scripts/agent-buy.mjs --dry-run       # 停在付款之前（省额度，见 §七）
node scripts/agent-buy.mjs --content 0xe2f8…   # 指定买哪一件
```

### 流程

| # | 这一步做什么 | 打印什么 | 靠哪条已经存在的东西 |
|---|---|---|---|
| 0 | 自检:环境变量齐不齐、地址与私钥对不对得上、AVAX/USDC 够不够 | 地址、两个余额、缺什么 | viem `getBalance` / `balanceOf` |
| 1 | **发现**:`GET /api/catalog` | 候选清单 + **它挑了哪一件、为什么** | W7 `api/catalog.ts` |
| 2 | **被拦下**:无头 `GET /api/content/:id` → **402** | 原始 402 body 里的 `accepts[0]` | W7 `api/content/[id].ts` |
| 3 | **解析报价**:从 402 里取出 `maxAmountRequired` + `quote` | 金额、`quoteId`、`expiresAt` | 同上 |
| 4 | 读 `allowance`,不够才 `approve` | 「额度够,跳过 approve」或「签第 1 笔」 | 人类路径同一条（`needsApprove`） |
| 5 | `pay(contentId)` | 两笔交易的 `txHash` + explorer 链接 | `CreatorSplitter.pay(bytes32)` |
| 6 | **带 `X-Payment` 重试**同一个 URL → **200** | 签名 URL + 字节数 | W7 `deliverBranch` |
| 7 | **真的下载一次** | 字节数 + `sha256`,`--verify-hash` 时与链上 `contentHash` 比对 | `getContent` 的 `contentHash`（W5 起是真哈希） |
| 8 | 收尾 | `txHash` + explorer 链接 + 最后一行机器可读 | 见下 |

### ⚠️ 第 4 步为什么要判 `allowance`，而不是无脑 approve

和人类路径同一个理由（`usePayFlow` 的 `needsApprove`）：授权额度够就**不该再弹一次**。
对 agent 而言这还多一层意义 —— **少一笔交易就少一次演示现场失败的机会**。
⚠️ 但**不能把"跳过"写死在报告里**:跳过与不跳过的输出必须能区分,
否则"演示跑了 2 笔还是 1 笔"这件事就说不清了。

### ⚠️ 第 6 步的 URL 必须与第 2 步**完全相同**

包括路径。签名覆盖 `contentId`,换一条路径就验不过 —— 那不是 bug,
是"报价绑定内容"在生效（W7 §5.4 第 4 条）。

### 第 8 步的输出格式（与 W7 那个验证脚本对接）

最后一行固定形如,便于直接喂给 `scripts/verify-x402.mjs`：

```
txHash=0xb8bc20f6…
payer=0x<agent>
```

`verify-x402.mjs` 会在链上把 `payer` 发过的、**还没被兑过**的付款扫出来当凭证
（用的是它的**无损探针**）—— 所以 W8 一跑,`W7-实施计划.md` §6.4.4 那几条
"验不了"的自动就有了材料。**两个脚本是配套的。**

### ⚠️ 必须处理的一条失败：`AlreadyPurchased`

合约 `CreatorSplitter.sol:215` 有 `if (purchases[contentId][msg.sender]) revert AlreadyPurchased(...)`
—— **同一个地址买同一件内容第二次必 revert**。而演示现场最可能的操作就是"再跑一遍"。

所以脚本**在发第 5 步之前先读 `purchases(contentId, agent)`**,
已经买过就**直接说明并给出出路**,而不是让钱包弹出来再 revert:

```
这件内容这个地址已经买过了(合约不允许重付)。
→ node scripts/agent-buy.mjs --content <另一件>
→ 或者用 /api/catalog 里其它在架内容
```

⚠️ 注意 `--dry-run` **不覆盖**这种情况:它停在付款前,所以"能不能付"这条它测不出来。
**自检里那次 `purchases` 读必须照做。**

---

## 四、看板标注怎么实现

改动很小,但有一处**必须小心**：

```ts
// useMyContents.ts —— Sale 加一个字段
isAgent: AGENT_ADDRESSES.includes(log.args.payer!),   // ⚠️ 大小写!
```

⚠️ **比的必须是归一化之后的地址。** `PaymentSplit` 事件里 `payer` 是 `indexed`,
viem 解出来是 **checksum 形态**;而白名单 JSON 里人手写的很可能是**全小写**。
直接 `includes` 会**静默地永不命中** —— 症状正好是"看板把 agent 标成人类",
也就是 §16.1 第 7 条那一屏挂掉。

**做法**:两侧都先 `getAddress(x.toLowerCase())` 再比（`shared/agentPay.ts:282`
对 `payer` 做的就是这件事,理由一模一样）。**别用 `===` 比裸字符串。**

界面上（`DashboardPage.tsx:205` 那一行）：

```
来自 0x9750…B988   [Agent]     ← 白名单命中
来自 0x1234…5678               ← 未命中,不加徽章
```

底部那句解释文字要**换掉**（现在写的是「买家类型标注(人类 / Agent)是 W8」）,
改成如实说明**判定方式**：「Agent 标记按地址白名单判定,名单内容:…」。

⚠️ **分账比例那一半不用做。** §16.1 第 7 条要求的是「**且分账比例与人类购买一致**」
—— 两条路走的是**同一个合约、同一个 `pay()`、同一个 `PaymentSplit` 事件**,
看板渲染的是事件本身。**这件事是结构上就成立的,没有代码要写。**
（这正是方案 §9.4 那句"v2.1 的改动全部落在 HTTP 层、合约零改动"的意思。）

---

## 五、"自主决策"必须是真的（反造假）—— 验收红线

§W8 那条注意值得单独成一节,因为它是**唯一一条"做对了也看不出来、做假了也看不出来"**的要求。

### 脚本里**不许出现**的三个值

| 值 | 必须来自 | 出处 |
|---|---|---|
| 要付多少钱 | `402` 响应的 `accepts[0].maxAmountRequired` | 不是常量、不是 `catalog` 里的 `price` 字段 |
| 要买哪一件 | `/api/catalog` 的返回,由脚本自己挑 | 不是命令行默认值（可以 `--content` 覆盖,但**默认必须是它自己挑的**） |
| 付款人是本人 | 本地私钥推导出的地址,**再回链上核对** | 不是写死的字符串 |

⚠️ 金额这条最容易写错:`/api/catalog` 里**也有** `price`,而且是同一个值,
所以"从 catalog 读价格"**看起来也对**、测起来也对。但那不是 402 协商的结果 ——
**它绕过了"被拦下 → 读报价 → 按报价付款"这个动作本身**,而那正是这一屏要展示的东西。

### 评委可以自己验的三步（写进 README）

1. 脚本输出里的 `txHash` 贴进 explorer → `PaymentSplit` 事件的 `payer` **就是**脚本自报的地址
2. 那笔交易的 `amounts` 求和 == 该内容当时的链上价格
3. 看板上这一笔带 `[Agent]`,且**分账明细与人类购买那一笔格式完全一样**

---

## 六、验证

### 与 §16.1 的对应

| §16.1 条目 | 本包 |
|---|---|
| ③ Agent 脚本能自主完成"发现 → 付款 → 取内容",无人干预 | `scripts/agent-buy.mjs` 一条命令 |
| ④ 该笔付款的 `txHash` 可在 explorer 当场验证 | 第 8 步打印 + README 三步 |
| ⑤ 同一个 `txHash` 第二次被拒 | **W7 已交付**;`npm run verify:x402` 可重跑 |
| ⑥ 冒用他人 `txHash` 被拒 | 同上 |
| ⑦ 看板标注买家类型为"Agent",且分账比例与人类一致 | §四 |

### 命令

```bash
# 1. 主路径（本机 dev server）
export AGENT_ADDRESS=0x…            # 公开地址
export AGENT_PRIVATE_KEY=0x…        # ⚠️ 只在当前 shell,用完关窗口
node scripts/agent-buy.mjs

# 2. 把它的产物喂给 W7 的反例矩阵
X402_PAYER=$AGENT_ADDRESS npm run verify:x402 --burn

# 3. 看板上人工看一眼徽章
```

⚠️ **第 2 步会补上 `W7-实施计划.md` §6.4.4 里那几条"验不了"** —— 前提是那台机器
**够得到 KV**。本机够不到 `*.upstash.io`（§6.3.3）,所以本机仍然只跑到 `503` 那一步。

### 6.1 实测记录（2026-09-24,本机）

**跑法**:用 anvil 那个**零余额**的公开测试地址（`0xa0Ee…9720`,链上只有 0.23 AVAX、
0 USDC）当 agent,`--dry-run`。选零余额地址是**故意的**:万一 `--dry-run` 有 bug 没停住,
它连 gas 都付不起,不会动到任何人的钱。

| # | 验的什么 | 结果 |
|---|---|---|
| 0 | 私钥推导地址、与 `AGENT_ADDRESS` 交叉核对、读 `splitter.usdc()`、报余额 | ✅ |
| 1 | 自己去 `/api/catalog` 挑"没买过的最便宜一件"（1 件在架,选中 `0xe2f8…`） | ✅ |
| 2 | 不带凭证 → `402` + `accepts[0]`（`100000` / `eip155:43113` / `eip3009-or-approve`） | ✅ |
| 3 | 从 `accepts[0]` 取金额、核对 contentId 绑定、报 `quoteId`/`expiresAt`/`sig` | ✅ |
| 4 | 读 `allowance` → 判"不够,要签 approve" → **停在签之前** | ✅ |

**`--dry-run` 真的没发交易**（不是"看起来没发"）:跑前跑后各读一次
`eth_getTransactionCount`,两次都是 `0x6369`。同一条路换了个**有 10.65 USDC** 的地址
跑,nonce 同样没动 —— 所以"演习"这件事是**可判定的**,不是承诺。

**一处代码级坑（已写进脚本注释)**:`estimateContractGas` 是 **public action**,
`createWalletClient` 上**没有**它（钱包客户端只有 `sign*`/`send*`/`write*` 那一族）。
所以第 5 步的 gas 预估走 `ctx.client.estimateContractGas({ account: ctx.agent, … })`。

### 6.2 看板徽章:三相位 + **负对照**（14/14）

`C:\Users\EmptyTouch\AppData\Local\Temp\pwshot\w8-agent-badge.js`

这条路线唯一的失效模式不是"徽章画错",而是**静默不命中**(风险 W8-3)。所以这个脚本
不满足于"看到徽章":它**同时**跑一条负对照 —— 直接对原始 JSON 数组做 `includes`
（就是那个错误写法),断言它返回 `false`,而 `isAgentAddress()` 返回 `true`。
两个结果并排打出来,"为什么必须归一化"就从一句注释变成了当场可见的事实。

- 相位 A 名单为空 → 0 枚徽章,说明写「名单现在是空的」
- 相位 B 用**全小写**地址登记 → 2 枚徽章,负对照 `naive.includes === false`
- 相位 C 还原 → 徽章消失(**证明真由那个文件驱动,不是写死的**)

另有一个**不改任何文件**的检查（`w8-whitelist-committed.js`,13/13):断言仓库当前
提交的那一版白名单被前端读到了 —— checksum / 全小写 / 全大写三种形态都命中,
另外两个角色不命中,`''`/`undefined` 安全返回 `false`。

### 6.3 控制台那张卡（17/17）

`w8-console-agent-card.js` 断言的重点不是"字在那儿",而是三条边界:

1. 卡里**恰好 2 个 `button`,且都是"复制命令"** —— 没有任何"运行 / 启动 / 一键"。
   这是决策 2 在界面上的体现:私钥不在服务端,网页不能代跑 agent。
   （第一版这里写成 `clicks.every(…)`,而定位落到了外层网格上、按钮数是 **0** ——
   空数组上 `every()` 恒为 `true`,断言**假通过**了。改成先断言 `length === 2`。）
2. 写着「按地址白名单判定」,没有写成"自动识别"。
3. 两行命令原样在,`--dry-run`「停在付款那一刻之前」说清楚了。

### 6.4 ⚠️ 没验到的（如实记）

- **第 5–8 步全部没跑** —— 要真花钱,而私钥只在**你那个 shell** 里。这是 §6 命令 1。
- **`allowance` 充足那条分支没走到**。实测了所有相关地址的 allowance,
  **全是 0**:`0x9750Af…`（付过两次)、`0xA0b7…`、三个新演示地址、anvil #1。
  原因是人类路径授权的是**恰好等于价格**的额度,`pay` 之后归零。
  → 所以**现实里每一场演示都是 2 笔交易**,那条"跳过 approve"的分支目前是
  **防御性的、不会走到**。别在演示里说"通常只要一笔"。
- **`503` / KV 那几条** 同 W7:本机 `*.upstash.io` 不通。
- **第 7 步的下载**在本机**注定失败** —— 私有 blob 的**数据面**从这台机器不可达
  （见验证手段记录)。脚本对这条**单独报**「验不了,不是没这回事」,
  绝不把它混进"签名坏了"。

### 6.5 一个**故意不加**的 `package.json` 别名

`verify:x402` 有别名,`agent-buy` **没有** —— 不是漏了。实测:

```
$ npm run t --dry-run        # → argv = []      ← npm 自己把 --dry-run 吃了
$ npm run t -- --dry-run     # → 才传得到脚本
```

npm 自己有一个 `--dry-run` 配置项。所以一个人只要顺手敲 `npm run agent:buy --dry-run`
（最自然的写法),**演习会变成真花钱** —— 而 `--dry-run` 恰恰是这个脚本唯一的刹车。
宁可少一个别名,也不要一个会把刹车踩成油门的别名。README 与那张卡上写的都是
`node scripts/agent-buy.mjs --dry-run`。

---

### 与 §十六 的偏差记录（追加）

- **§十 第 5 步**说的「第三个入口」落成了控制台上的一张卡,而**不是**顶栏的新页签。
  理由同 §八:agent 不用网页,给它一个页签就是造一个没有内容的页面。
  顶栏那个灰着的 `Agent` 页签(`Shell.tsx`,note「W7 起」)已按上面 ② 删掉。

---

## 七、风险

| # | 风险 | 影响 | 应对 |
|---|---|---|---|
| W8-1 | **私钥手滑进仓库** | 🔴 最高 | 只用 shell export;`.gitignore` 兜不住手写文件,所以**提交前 grep 一遍** `0x[0-9a-f]{64}`;`.env.example` 只写变量名 |
| W8-2 | faucet 额度耗尽,**演示中途** | 🟠 演示失败 | 三个角色三个地址;脚本**自检里先报余额**;`--dry-run` 省额度;演示前提前累计（Circle 限的是**每地址** 1 USDC / 2h,**不限地址数**,多备地址是线性扩容） |
| W8-3 | 白名单大小写不归一 → **永不命中** | 🟠 验收项 ⑦ 静默挂掉 | §四 那条:两侧 `getAddress(toLowerCase())`,**别用裸字符串比** |
| W8-4 | 脚本"自主"是假的（硬编码金额/内容） | 🔴 被怀疑造假就全废 | §五 三条禁令;README 写清评委怎么自己验 |
| W8-5 | 演示现场 RPC 抖动 | 🟠 | 脚本沿用 `shared/chain.ts` 的主/备两个端点;失败要**明确说是 RPC**,别吞成一个笼统的报错 |
| W8-6 | agent 地址与人类买家地址混用 | 🟠 额度互相挤 | 决策 1;自检里**核对** `AGENT_ADDRESS` 是否等于演示用的另两个角色地址,是就拒绝运行 |
| W8-7 | 重跑撞 `AlreadyPurchased` | 🟠 现场尴尬 | §三 那条:发交易前先读 `purchases`,并给出出路 |

---

## 八、明确不做

- **不做 MCP 封装 / 多 agent / 子图索引** —— 方案 §17.1 列为加分层,只在 W11 全绿后才动。
- **不做"网页上点一下就跑 agent"** —— 那要求把私钥搬到服务端,直接违反决策 2。
- **不做 KV 双信号标注**（§〇 备选）—— 对演示没有增量。
- **不改合约** —— 本包一行 Solidity 都不动。这是 §9.4「v2.1 的改动全部落在 HTTP 层」的兑现。
- **不做 agent 的"预算控制 / 策略"** —— 那是 x402 生态的话题,不是本演示要证明的事。

---

## 九、与上游的差异记录（**追加，不覆盖原文**）

### 1.（方案 §9.4 第 706 行）给的两条路里,**② 不可用**

原文让 ① ② 二选一,并推荐 ①。**② 是错的不是次优的** ——
本项目的 agent 自己发两笔交易,**有**钱包签名交互,拿 ② 判会把它判成人类。
上游若再引用这段,请连同本条一起看。

### 2.（方案 §9.4 第 706 行）遗漏了第三条路

规格只列了"白名单"与"有没有钱包交互"两种,没提**KV 里那条 `payment:` 记录**
（只有 agent 路径会写,不可伪造）。本包评估后**仍选 ①**,理由见 §〇 ——
但这条"记录在案"是有意的:它答的是另一个问题,将来若要讲"交付路径可审计",
它是现成的材料。

### 3.（开发计划 §W8 第 401 行）「§10:MVP 用地址白名单」的**代价要补一句**

原文只写了"简单直接",没写它的边界:**白名单是身份标注,不是行为检测 ——
名单外的 agent 会被标成人类。** 这个代价必须显示在界面上,不能只写在文档里。

### 4.（开发计划 §R9）价格 0.2 USDC 与现状不符

实际在架演示内容 `0xe2f8dba7…` 是 **0.1 USDC**。额度换算按实际值。
⚠️ **不覆盖原文** —— 只是提醒别拿那句去算 faucet 够不够。

### 5.（`src/pages/ConsolePage.tsx:125`）一条从 W7 挪过来的 TODO

那个注释说第三个入口「**W7 时改**」,W7 交了 API 但没动它。本包接手（§二）。
**挪包的理由**:那个入口的内容是"看 Agent 自主购买",而在 W8 之前
**没有可看的东西** —— 在 W7 做就等于挂个假按钮,注释自己也是这么说的。

---

## 十、开工顺序（建议）

> 进度（2026-09-24):1–5 全部落地;**6 只做了一半** —— 命令 1（真花钱那一段）
> 要你的私钥,得你来跑;命令 2 本机够不到 KV。详见 §6.1–§6.5。

1. ✅ `shared/agentAddresses.json` + `.ts`（先落唯一来源,后面的都依赖它）
2. ✅ `src/hooks/useMyContents.ts` + `DashboardPage.tsx` 的徽章（**最小、立刻可见**,且能独立验）
3. ✅ `scripts/agent-buy.mjs` 的**只读前半段**（第 0–3 步:自检 → catalog → 402 → 解析报价）
   —— 这一段**不花钱**,可以反复跑到满意
4. ✅ 后半段（approve → pay → 交付 → 下载）—— **写好但只跑到第 4 步**,见 §6.4
5. ✅ `ConsolePage` 第三个入口 + `README`（入口做成控制台上的**一张卡**,不是新页签,理由见末节）
6. ⏳ 跑 §六 那三条命令;把结果**追加**回本文件（与 W7 的 §6.3/§6.4 同一条纪律）

### ✅ 2026-09-24 两处岔口已拍板

**① 演示内容:用新创作者 `0xAa05f680…` 建一件。**

链上**唯一在架**的那件 `0xe2f8dba7…` 是老创作者/部署者 `0xA0b760DC…` 建的,
而新创作者名下什么都没有 —— 它的看板打开是空的。但 §16.1 第 7 条要看的
恰恰是「**一页上**人类那一笔和 agent 那一笔并排、分账比例一致」,空看板演示不了。

所以:**用 `0xAa05f680…` 通过 `/create` 建一件并定价**(建议 0.1 USDC,与现有的对齐),
然后人类买家先买一笔、agent 再买一笔。这一件内容就是整个演示的舞台。
⚠️ 建它要花这个地址的 gas 和 faucet 额度 —— 那是演示预算里该有的。

**② 顶栏那个灰着的 `Agent` 页签:删掉。**

删的理由不是"没时间做",是**它许诺的东西不该存在**:一个网页按钮跑不了 agent,
因为私钥按方案 §6.2 只存在于跑脚本那个 shell 里,搬到服务端就违反了决策 2。
第三个入口现在长在控制台的「Agent 接入」卡上。
(备选「改成指向那张卡 / 保留但改 note」都没选:前者让同一页出现两个导航项,
后者留着一句永远不兑现的承诺。)

---

## 十一、`/api/catalog` 又慢又偶发 503 —— 根因查清了,而且**不是**最初以为的那个

> 2026-09-24。**本节第一版把根因判错了,先列更正再给结论。**
> 涉事代码:`server/chain.ts` / `api/catalog.ts`(W7)、`src/hooks/useMyContents.ts`(W5)。
> 记在本文件是因为它是在 W8 的复现过程里暴露的,且挡的正是 §16.1 第 7 条那块屏。

### 11.0 ⚠️ 更正:第一版错在哪(留档,不删)

| 第一版的说法 | 实际 |
|---|---|
| 「主端点 `api.avax.test.network` DNS 解析不了」 | **是我自己的笔误。** 源码 `shared/chain.ts:124` 写的是 `avax-test`(连字符),解析正常、24/24 通过。我在探针里多打了一个点 —— 那个主机名根本不存在,**于是"量到"了一个我自己造出来的故障** |
| 「503 的成因是主备不成对:备端点拒收 158k 全量,fallback 抛的错不被吞」 | **没有任何证据支持。** 正常进程里那对主备 6/6 通过(0.4–2.1s);线上部署 6/6 @442–595ms |
| 「老 `vercel dev` 进程攥着 DNS 坏之前建的连接池,所以 ~25%」 | **重起一个全新进程一样坏**(16–125s,照样 503)。与进程无关 |
| 更早说的「看到 503 直接重跑就行,**不是 bug**」 | 收回。它**是** bug —— 只是根因在下面这条,不在 RPC |

三条错的**共同形状**:我把一个**环境**问题读成了**代码**问题,而且在没做对照实验之前就写进了文档。
一条对照就能拦住前两条 —— **把同样的调用在 shell 里跑一遍**。这一条比本节任何内容都值得记。

### 11.1 真根因:本机出网要走本地代理,而只有浏览器走了

这台机器上有一个本地代理 **`127.0.0.1:7897`**,WinINET 里是开着的
(`ProxyEnable=1` / `ProxyServer=127.0.0.1:7897`)。于是:

| 谁 | 走不走代理 | 结果 |
|---|---|---|
| Edge / 浏览器 | ✅(Chromium 读 WinINET) | 部署首页 2.36s;`/api/catalog` 6/6 @**442–595ms** |
| curl / node(我的 shell) | ❌ 没有 `HTTP(S)_PROXY`,WinHTTP 是"直接访问" | `splitjar.vercel.app` → `UND_ERR_CONNECT_TIMEOUT`;curl 21s 后 000 |
| `vercel dev` 里的函数 | ❌ | 每次出网 **4–70s**,viem 的超时预算被耗尽 → 503 |

**一次对照就说完了这件事:**

```
node           不带代理 → ✗ UND_ERR_CONNECT_TIMEOUT  10729ms
node           带代理   → ✅ HTTP 200                  690ms  items=2
curl           带代理   → ✅ /api/health 200 @0.73s
```

⚠️ **`node` 的 `fetch`(undici)**默认不读** `HTTP_PROXY`。** Node 24 起要显式开:
`node --use-env-proxy`,或 `NODE_USE_ENV_PROXY=1`。
**不记这一条,演示当场会卡在 agent 脚本第 1 步。**

同一个根因顺手解释了一串旧现象,它们**不是各自的毛病**:
`https://vercel.com/` 本机 30s 超时;`vercel dev` 有一次起不来(`Retrieving project… fetch failed`);
`*.vercel.app` 被"劫持"到 `98.159.108.61`(真实地址 `216.198.79.67` / `64.29.17.67`,
**钉住真 IP 直连也是 000**);以及更早记下的「CoinGecko 不通」。

### 11.2 503 的机制(直接证据)

插桩(已撤)逮到的那一次:

```
[probe] 扫链抛了 @41615ms: The request took too long to respond.   ← 同一轮里那次 HTTP 503 @44.5s
```

`扫链` = viem 的累计超时预算(主备各一次 + `retryCount: 1`)被函数内的慢出网耗尽,于是抛
`TimeoutError`,落进 `api/catalog.ts` 的 `catch {}` → 我们自己那个 503 `upstream_unavailable`。
**机制是清楚的;它只是被 11.1 那条环境问题触发的,不是被代码触发的。**

同一次插桩的 12 条读数(不带代理),三段落**每一段都能卡**,不是同一个目的地:

| 段 | 范围 | 说明 |
|---|---|---|
| 扫链 | 4.0–35.0s | 在 shell 里同样三次调用是 **0.8–3.7s**(24/24 无卡顿) |
| KV(`getContentTitles`) | 0.9–46.4s | 失败被吞(标题是装饰),但**时间照花** |
| Blob(`loadPreviewUrls`) | 0.6–69.3s | 同上 |

**带代理之后**:本地 `/api/catalog` 6/6 成功,5.1–9.3s(对照:不带代理 16–125s、约 1/4 是 503)。
⚠️ 那 ~5s 的底噪是 `vercel dev` 自己的固定开销 —— 零 I/O 的 `/api/health` 也要 2.5s。

### 11.3 仍然成立的那条:备端点拒收全量(潜在缺陷,**不是今天的成因**)

```
{"error":{"code":-32701,"message":"exceed maximum block range: 50000"}}
```

`DEPLOY_BLOCK = 58_513_443` → `latest ≈ 58_671_844`,跨度 **158,401 = 上限的 3.17 倍**,且只涨不减。

- **主端点能一次扫完**(3/3,1.1–1.5s)⇒ 今天任何现象都不是这条造成的。
- **但它是真的**:主端点一旦不可达,`fallback` 是**硬失败**,不是降级 ——
  而"主备"这整套东西的全部意义,恰恰是在主端点不可达那一刻兑现。
- 所以分块扫**照做**(已拍板),但要如实给它的定位:**稳健性修复,不是阻塞项。**

### 11.4 改动与代价

- 新增 `shared/blockWindows.ts`(纯函数 `blockWindows` + `inWindows`,常量 `LOG_WINDOW_SIZE = 50_000n`)
- 6 处调用点:`server/chain.ts` `listRegisteredContents` / `listActiveChanges`(改为收一个**传进来的 `toBlock`**)、
  `src/hooks/useMyContents.ts` 三处、`scripts/verify-x402.mjs` 一处
- 顺手修一处**正确性**问题:`toBlock` 从 `'latest'` 改成钉死的块号 ——
  否则 `Promise.all` 的三次读可能落在三个不同高度,响应里的 `blockNumber` 是一句没有依据的话
- **代价 A/B**(同一环境,带代理):分块 **5.1–6.6s** vs 单窗口 **5.05–6.4s** ⇒ **测不出差异**,
  被 `vercel dev` 那 ~5s 固定开销盖住了。**线上的差异要等部署后再量**

### 11.5 对演示的结论(本节最要紧的一条)

1. **彩排与演示都打在部署上**,不要打本地:`https://splitjar.vercel.app`
2. 任何 node/curl 打部署的动作**必须带代理**:
   ```
   export HTTPS_PROXY=http://127.0.0.1:7897
   AGENT_PRIVATE_KEY=0x… BASE_URL=https://splitjar.vercel.app \
     node --use-env-proxy scripts/agent-buy.mjs --content 0x91ffa529…
   ```
3. 本地 `vercel dev` 仍可用(改代码、看界面),但要么带 `NODE_USE_ENV_PROXY=1`,
   要么接受它慢十倍并且会 503
4. 本机浏览器是唯一"天然带代理"的通道 —— 这也解释了为什么此前所有前端验证都只能在浏览器里做

### 11.6 不在这次修的

- **KV 游标 / TTL 缓存**:能把"每次全量扫"降成"只扫增量",是正解。但它引入 KV 依赖,
  且**首次仍要全量** —— 不先解决 50k 上限它一步都走不了。顺序是"先分块,再谈游标",记档不丢。
- **本地 `vercel dev` 的固定开销**(2.5s/请求,零 I/O 也一样):工具链的事,不是我们的代码。
- 预览图缺口(§九 第 5 条)仍是缺口。

### 11.7 残余风险

| 风险 | 判断 |
|---|---|
| **演示机没开代理** | **本次最高的风险。**彩排前先用 `curl -x` 验一次线上能不能通,验不过就别开始 |
| 两个 RPC 端点同时挂 | 无解。但窗口化之后,备端点不再是"必然失败" |
| 12 并发被限流 | 本次 12/12 通过;事件数上到两位数时要重测 |
